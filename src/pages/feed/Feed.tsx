import { FC } from 'react'
import styles from './styles.module.css'

type TProps = {
  
}

export const Feed: FC<TProps> = ({  }) => {
  return (
    <div className={styles.feed}>
        Feed
    </div>
  )
}