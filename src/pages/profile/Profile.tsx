import { FC } from 'react'
import styles from './styles.module.css'

type TProps = {
  
}

export const Profile: FC<TProps> = ({  }) => {
  return (
    <div className={styles.profile}>
        Profile
    </div>
  )
}